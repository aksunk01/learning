/** @type {import('next').NextConfig} */
const nextConfig = {
  allowedDevOrigins: [
    'macbook',
    'macbook:3000',
    '*.ts.net',
    'localhost',
    'localhost:3000'
  ]
};



export default nextConfig;
